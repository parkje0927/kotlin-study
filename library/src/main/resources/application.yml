spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/library?allowPublicKeyRetrieval=true&useUnicode=true&useSSL=false&serverTimezone=Asia/Seoul
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # 포맷 형식
        highlight_sql: true # sql 하이라이팅
#        use_sql_comments: true
        show_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
